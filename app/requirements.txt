# GitHub Copilot SDK for Python (public preview, not yet on PyPI).
# Package name is `github-copilot-sdk` per the project's pyproject.toml.
# The `[telemetry]` extra installs opentelemetry-api which the SDK uses
# for trace-context propagation between the CLI subprocess and your code.
# See https://github.com/github/copilot-sdk/tree/main/python
github-copilot-sdk[telemetry] @ git+https://github.com/github/copilot-sdk.git#subdirectory=python

# Pydantic models for the @define_tool decorator's JSON schema generation.
pydantic>=2.7.0

# Azure AI Search client (used by retrieve_docs and seed_index.py).
azure-search-documents>=11.5.0

# Azure OpenAI via the official OpenAI Python SDK.
openai>=1.40.0

# Passwordless auth to Azure resources (Search, AOAI, App Insights).
azure-identity>=1.18.0

# Reading .env produced by `azd env get-values`.
python-dotenv>=1.0.0

